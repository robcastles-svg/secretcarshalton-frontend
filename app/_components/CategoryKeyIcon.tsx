/** The key from the site logo, reused after category headlines for consistent branding — purely decorative. */
export function CategoryKeyIcon() {
  return <img src="/key-icon.png" alt="" className="category-key-icon" />;
}
